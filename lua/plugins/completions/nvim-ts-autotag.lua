-- Tap "cw" in normal mode when the cursor in under une html tag to change the tag name with this pair

return {
  "windwp/nvim-ts-autotag",
  lazy = false,
  config = {},
  dependencies = {
    "nvim-treesitter/nvim-treesitter",
  },
}
